# Copy to .env and fill in. Every variable is marked REQUIRED or OPTIONAL.

# --- Model access (REQUIRED) -------------------------------------------------
# Hermes reaches Anthropic through Claude Code credentials (PLAN.md D46).
# Generate a long-lived token once with: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=

# --- Web search (REQUIRED) ---------------------------------------------------
TAVILY_API_KEY=

# --- Telegram bot (NOT REQUIRED) ---------------------------------------------------
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USERS=

# --- A2A caller identities (REQUIRED) ----------------------------------------
# Each token identifies the CALLER. orchestrator calls the experts; the experts call researcher.
# Generate each with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
A2A_TOKEN_ORCHESTRATOR=
A2A_TOKEN_RECIPE_EXPERT=
A2A_TOKEN_COST_EXPERT=
A2A_TOKEN_MARKETING_EXPERT=

# --- kitchen-ledger agent tokens (REQUIRED) ---------------------------------------
# One token per agent, distinct from the A2A tokens. docker-compose.yml joins them into the server's
# LEDGER_AGENT_TOKENS (agent:token,agent:token) and gives each agent its own as LEDGER_TOKEN.
LEDGER_TOKEN_ORCHESTRATOR=
LEDGER_TOKEN_RECIPE_EXPERT=
LEDGER_TOKEN_COST_EXPERT=
LEDGER_TOKEN_MARKETING_EXPERT=

# --- App Postgres (REQUIRED) -------------------------------------------------
POSTGRES_PASSWORD=
# OPTIONAL: host port for tests and `make db-shell` access (default 55432; 5432 is often taken locally)
POSTGRES_HOST_PORT=55432

# --- Hermes API server on orchestrator (REQUIRED from Loop 4: self-test and evals) ----
API_SERVER_KEY=

# --- Telegram (OPTIONAL) -----------------------------------------------------
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USERS=

# --- Guardrails and limits ---------------------------------------------------
KITCHEN_GUARD_TIMEOUT_SECONDS=10
# OPTIONAL. How guard calls authenticate under D46 is PLAN.md open question 2.
KITCHEN_TURN_COST_CAP_USD=5.00
KITCHEN_IMPORT_DIR=/opt/data/cache/documents

# --- Cockpit (REQUIRED from Loop 5) ------------------------------------------
# Agents and kitchen-ledger POST their events to http://cockpit:8080 with this token.
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
KITCHEN_COCKPIT_TOKEN=

# --- Langfuse v4 self-hosted headless init (REQUIRED from Loop 5) ------------
# Created on the first `make up`; the agents send traces with the same project keys.
# Keys must start with pk-lf- / sk-lf- followed by a random suffix, e.g.
#   echo "pk-lf-$(openssl rand -hex 16)"   and   echo "sk-lf-$(openssl rand -hex 16)"
LANGFUSE_INIT_ORG_ID=kitchen
LANGFUSE_INIT_PROJECT_ID=kitchen-assistant
LANGFUSE_INIT_PROJECT_PUBLIC_KEY=pk-lf-
LANGFUSE_INIT_PROJECT_SECRET_KEY=sk-lf-
LANGFUSE_INIT_USER_EMAIL=
LANGFUSE_INIT_USER_PASSWORD=

# --- Langfuse stack secrets (REQUIRED from Loop 5) ---------------------------
# Every CHANGEME secret of the official Langfuse compose file. Generate each with: openssl rand -hex 32
# (that prints 64 hex characters, the exact format LANGFUSE_ENCRYPTION_KEY needs).
LANGFUSE_NEXTAUTH_SECRET=
LANGFUSE_SALT=
LANGFUSE_ENCRYPTION_KEY=
LANGFUSE_POSTGRES_PASSWORD=
LANGFUSE_CLICKHOUSE_PASSWORD=
LANGFUSE_REDIS_AUTH=
LANGFUSE_MINIO_ROOT_PASSWORD=
# OPTIONAL host ports, all bound to 127.0.0.1
LANGFUSE_HOST_PORT=3000
LANGFUSE_MINIO_HOST_PORT=9090
COCKPIT_HOST_PORT=8080
